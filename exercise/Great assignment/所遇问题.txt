1.p=root的归位，有一处忘写；
2.if...else if 容易写出逻辑漏洞，要小心（加上期末考还有以前，不知多少次了）
3.pos[Snum]=&pos[Snum++];这里这个++会在取地址前加上
4.cmp在qsort中运作原理：qsort会实打实用cmp排区间内记录，cmp中指针似乎不能指向区间别处，会有奇怪的值
5.if(-1)...  视为真
6.又一次碰上re，不过幸好只是数组开的不够大